# Patch generated by Pyment v0.4.0dev

--- a/income.py
+++ b/income.py
@@ -5,12 +5,18 @@
 def turnover_error(y_true: np.array, y_pred: np.array) -> float:
     """
 
-    Args:
-        y_true (np.array): _description_
-        y_pred (np.array): _description_
+    Parameters
+    ----------
+    y_true : np.array
+        _description_
+    y_pred : np.array
+        _description_
 
-    Returns:
-        float: _description_
+    Returns
+    -------
+    float
+        _description_
+
     """
     y_t = np.where(y_true >= 0, 0, y_true)
     y_p = np.where(y_pred >= 0, 0, y_pred)
